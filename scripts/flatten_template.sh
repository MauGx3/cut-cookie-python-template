#!/usr/bin/env sh
# ARCHIVED: flatten_template.sh
# This helper script was used temporarily during template flattening and has been archived.
# If you need to re-run the same flattening operation, restore it from the repository history
# or from the archive `flat-template-archived-by-agent/`.

echo "This helper script has been archived and should not be used in the repository root."
exit 0
if [ ! -d "$SRC_DIR" ]; then
